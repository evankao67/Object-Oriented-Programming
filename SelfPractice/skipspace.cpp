#include<bits/stdc++.h>
using namespace std;
typedef long long ll;
#define ln '\n'
int main()
{
    char x;
    cin.unsetf(ios::skipws);//when skip white space, system catch the space before input
    cin>>x;
    cout<<x<<ln;
    cin.setf(ios::skipws);
}